public class bitmanipulation {
    public static void main(String[]args){
            int no =5;
            int pos =2;
            int bitmask=1<<pos;
            //getbit;
            /*if((bitmask & no)==0) {
                System.out.println("bit is zero ");
            }else{
                System.out.println("bit is one");
            }
        //setbit;
        int abc=bitmask|no;
         System.out.println(abc);
         //clearbit;
         int notbitmask=~bitmask;
         int noue=notbitmask&no;
         System.out.println(noue);*/
         //update bit mask;
         //for 0=clear bit;
         //for 1=set bit;
    }
}
